# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
.venv-*/
venv/
ENV/
env/

# Environment and secrets
.env
.env.local
*.pem

# Local config (project uses .env + src/core/config.py)
config/

# Local SQLite DBs (embedded/examples)
*.db
cml_memory.db

# IDE
.idea/
.vscode/
.cursor/
.claude/
.agents/

# Testing
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.tox/
.nox/

# mypy
.mypy_cache/

# Ruff
.ruff_cache/

# uv
.uv/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/
.yarn/

# Evaluation (outputs, logs; large generated files)
evaluation/locomo_plus/data/unified_input_samples_v2.json
evaluation/outputs*/
evaluation/**/*.log

# Celery
celerybeat-schedule
celerybeat.pid

# Logs
*.log

# Profiling
*.prof
*.pstats
*.lprof

# Temporary / scratch
*.tmp
*.bak
*.pid
*.swp
*.swo

# SQLite variants
*.sqlite
*.sqlite3

# OS
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# Recovered corrupt git backup (do not track)
.git.corrupt/

# Unreleased features
packages/models/datasets/
packages/models/prepared_data/

# Generated local training outputs (tracked canonical artifacts may still appear
# as modified, but new task-specific outputs should stay out of git status)
packages/models/trained_models/*_epoch_stats.json
packages/models/trained_models/*_metrics_*.json
packages/models/trained_models/*_report_*.json
packages/models/trained_models/*_training_metadata.json

# Quality test results
scripts/quality_test_results.json
artifacts/

# Model binaries — hosted on Hugging Face Hub (avinashm/CognitiveMemoryLayer-models)
# Download with: huggingface-cli download avinashm/CognitiveMemoryLayer-models
packages/models/trained_models/*.joblib
packages/models/trained_models/**/*.safetensors

# Local trainer scratch directories
packages/models/trained_models/checkpoint-*/
packages/models/trained_models/**/checkpoint-*/
packages/models/trained_models/runs/
packages/models/trained_models/wandb/

# ML tooling run directories
mlruns/
lightning_logs/

# Coverage JSON reports (generated; tracked historical copies use git rm --cached to untrack)
full_cov.json
full_repo_cov.json
full_repo_cov_mock.json
unit_coverage.json

# Evaluation local env overrides (copy of *.example; never commit secrets)
evaluation/locomo_plus/scripts/env.local.sh
evaluation/locomo_plus/scripts/env.sh.local