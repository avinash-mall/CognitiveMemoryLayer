# Normalize line endings for CI (ruff format --check on Linux)
*.py text eol=lf
# Model binaries are hosted on Hugging Face Hub — not tracked in git
# See README.md § "Downloading Trained Models" for download instructions
